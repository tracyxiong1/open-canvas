{
  "name": "open-canvas",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/preview"
  ],
  "scripts": {
    "pack:cli": "node -e \"require('node:fs').mkdirSync('artifacts', { recursive: true })\" && npm pack --workspace open-canvas-cli --pack-destination artifacts",
    "test:package": "npm run test:package --workspace open-canvas-cli",
    "build": "npm run build --workspaces --if-present",
    "check": "npm run build --workspace @open-canvas/core && npm run check --workspaces --if-present",
    "dev": "npm run dev --workspace @open-canvas/preview",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  },
  "version": "0.1.1"
}
