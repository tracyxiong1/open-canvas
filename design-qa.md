# Open Canvas 画布视觉与交互验收记录

状态：进行中
更新日期：2026-08-21

## 一、验收范围

本记录覆盖 Open Canvas 本地版的无限画布。外部画布仅用于采集布局、信息密度与交互反馈，不复制其品牌、账户体系、在线资源、素材、接口或运行时代码。

当前本地版边界如下：

- 角色、场景和风格以当前项目中的普通上下文节点存在。
- 图片、视频等素材由 CLI 导入当前项目，并由画布节点引用。
- 不提供跨项目的角色库、素材库、风格库、在线模板市场或独立历史面板。
- 账户、空间、协作、计费、额度、在线任务中心和在线资源中心不属于本轮验收。

## 二、已核验状态

| 状态 | 本地实现 | 核验结果 |
| --- | --- | --- |
| 默认多节点画布 | 11 个语义有效节点、10 条扇出依赖边、点阵背景和固定控制层 | 已在 1440 × 900 同视口下比对。首节点、文本节点、扇出边、默认缩放和中心底栏几何已对齐。 |
| 图像/视频节点选中 | 节点聚焦、参数编辑器、项目内当前素材缩略图与结果预览；编辑变体栏与按需输出设置 | 已在 `1280 × 720` 同视口下比对。参数区由先前的左侧裁切修正为约 `101,481,660×203`；参考状态约为 `100,481,660×205`。图像/视频节点可在不改变默认密度的前提下修改可执行画幅，变化经过项目文档、失效传播和撤销/重做。 |
| 文本节点选中 | Prompt 编辑、上游素材缩略图与连接计数 | 参数区使用真实上游输出资产，不展示提供方、额度或在线模型信息。 |
| 添加节点菜单 | 创作节点与“添加本地素材引用”入口 | 菜单几何和出现位置已比对；菜单不包含角色库、素材库、风格库或历史记录。 |
| 画布导航 | 空白处拖拽、抓手或双指滑动平移；`⌘/Ctrl` + 滚轮或双指捏合缩放；缩放读数与适配视图 | 浏览器实际拖拽确认节点与其参数区一起移动，且它们的相对偏移保持不变。 |
| 节点移动 | 选中工具下直接拖拽节点、未保存状态、撤销/重做 | 浏览器实际拖拽确认节点位置发生预期变化，且不触发画布平移。 |
| 节点连线 | 统一左右端口、边显隐、网格吸附与依赖语义 | 浏览器实际操作确认角色上下文连接到图像镜头后，边数量从 10 变为 11，并显示“已连接 1 项”。 |
| 节点删除 | Delete/Backspace 删除聚焦节点，清理其连线与任务，并使直接受影响的下游失效 | 浏览器实际操作确认删除左侧图像节点后节点和相关连线消失，出现“已删除节点，相关下游已标记为待生成”；点击撤销后节点和连线恢复。 |
| 合成/输出节点 | 独立的“合成输出”节点；展示上游节点数量、完成状态和本地成片渲染边界 | 浏览器实际操作确认可从添加菜单创建、从镜头端口连接，参数区随即从“等待连接镜头”变为“已接收 1 个上游节点”。 |
| 项目内角色/素材引用 | 角色、风格、素材引用节点的 Prompt 和本地资产关联；图像/视频镜头可直接选择项目内参考素材 | 角色节点不打开全局库；已导入资产可在节点内关联。真实浏览器已验证镜头选择素材后结果转为待生成并显示关联数量。 |
| 节点内历史结果 | 生成节点可查看被后续版本替代的成功输出，并将其中一个结果作为当前镜头的项目内参考素材 | 自动化验证：历史结果不重复当前输出；选择旧结果后，镜头进入待生成并复用既有的参考素材失效/撤销链路。 |
| CLI ↔ Studio 本地项目 | CLI 打开实际项目；Studio 只在显式保存时通过回环桥接写回 | 真实浏览器已验证项目名、草稿和节点来自 CLI 项目；编辑镜头 Prompt 后保存，CLI 读取到同一份新版本。 |

## 三、实现边界与有意差异

以下差异属于本地版的既定范围，不计为视觉或交互缺陷：

1. 顶栏使用本地项目、草稿、导入/导出和保存状态，未显示账号、会员、额度、空间或在线 Agent。
2. 底部和左下控制层保留画布操作：添加节点、选择/抓手、自动整理、连线显隐、网格吸附、撤销/重做、缩放和快捷键帮助。
3. 在线素材库、角色库、历史资产、教程市场等入口由项目内节点、CLI 导入和撤销/重做替代。
4. 生成参数显示本地文档中可执行的提示词、输出规格和引用关系；不伪造提供方名称、点数、额度或在线状态。
5. 图标使用开源图标库中的通用图标，演示素材使用 Open Canvas 本地资源。

## 四、自动化与构建验证

2026-08-20 本执行环境已执行：

- `npm run check`：通过。
- `npm run test --workspace @open-canvas/preview`：通过，51 项。
- `npm run build --workspace @open-canvas/preview`：通过。
- `git diff --check`：通过。

根目录全量测试仍包含需要在 `127.0.0.1` 建立回环服务的 CLI 桥接用例；当前受限执行环境会在监听阶段返回 `listen EPERM`，因此不把该环境的该两项用例误记为产品失败或已验证通过。

React Flow 在 jsdom 的 MiniMap 测试中仍会输出非阻塞的 SVG `NaN` 警告。浏览器实测的画布、小地图、端口和拖拽未出现对应渲染问题。

### 4.1 节点内历史增量验证（2026-08-20）

- Core：39 项通过。
- Preview：34 项通过；新增覆盖“旧成功任务不重复当前输出”和“将历史结果安全复用为当前镜头参考素材”。
- Preview 生产构建与 `git diff --check`：通过。
- 当前受限执行环境拒绝在 `127.0.0.1` 监听端口（`listen EPERM`），因此 CLI 全量中的两项回环桥接测试无法在此环境启动；其余 8 项 CLI 测试通过。这是环境网络限制，不是桥接或画布断言失败。
- 同一限制也阻止本轮为节点内历史新开本地浏览器页面；历史 UI 的交互通过 jsdom 的真实组件树回归，已有 CLI ↔ Studio 回环桥接的浏览器验证记录保留在上表。下一次在开发机运行时应补录历史选择层的可视化状态。

### 4.2 键盘入口增量验证（2026-08-20）

- 修复了控制层标注与实际行为不一致的问题：`N` 打开节点菜单并把焦点移到首个可选节点，`Alt+Shift+F` 打开自动整理预览。
- 对话框、菜单与文本输入获得优先级；快捷键不会吞掉正在编辑的 Prompt。
- `npm run check` 与 Preview 的 35 项回归测试通过；React Flow 的 jsdom MiniMap 仍有既有、非阻塞的 `NaN` SVG 警告。

### 4.3 项目内上下文、边与媒体预览增量验证（2026-08-20）

- 角色设定、场景/风格、本地素材引用、编辑、分析和音频节点共用当前项目的素材关联能力；没有新增全局角色库或素材库入口。
- 单击连线可选中，`Delete`/`Backspace` 可只删除选中连线；拖到不兼容端口会给出安全提示，取消拖拽不提示错误。
- 聚焦节点可用 `⌘/Ctrl+D` 创建独立的项目内副本；失败镜头可按需查看安全错误摘要和下一步建议。
- 真实项目中的 MP4 在节点、输入引用、节点内历史和详情预览中通过本地 `<video>` 播放；演示数据的 WebP 封面继续作为静态预览，避免把封面当作媒体文件播放。
- `npm run test --workspace @open-canvas/preview`：41 项通过；`npm run check`、Preview 生产构建和 `git diff --check`：通过。由于上述回环端口限制，本轮没有将这批新增状态标记为新的浏览器实测结论。

### 4.4 基础多选增量验证（2026-08-20）

- 支持 `Shift` 空白框选与 `⌘/Ctrl` 点击追加/取消选择；未按修饰键的空白拖拽仍保持平移画布。
- 多选卡片同时展示选中反馈，只有主选节点展开随节点移动的参数区，避免多个参数层互相遮挡。
- 拖动选区会将 React Flow 的每个已移动节点坐标同时映射回项目文档；`Delete`/`Backspace` 将整组节点通过共享命令核心删除并保留撤销链路。
- 新增 Preview 回归覆盖“主选节点参数区”和“修饰键多选后整组删除”；模型层覆盖组拖拽的坐标换算。当前受限环境无法启用本地回环服务器，浏览器实测仍留待开发机补录。

### 4.5 项目内节点组复制/粘贴增量验证（2026-08-20）

- `⌘/Ctrl+C` 只写入当前画布会话的节点结构、布局和选区内部连线；不会读取或写入系统剪贴板。
- `⌘/Ctrl+V` 只在同一草稿中以偏移位置创建新节点，并只恢复新节点之间的内部连线；切换草稿会清空该会话内的节点剪贴板，粘贴节点均从待生成状态开始。
- 引用当前项目素材的 `inputAssetIds` / `referenceAssetIds` 保持为同一项目 ID，不复制素材字节、任务、生成输出或凭证。
- 粘贴后不强制把旧多选态迁移到新 ID，避免 React Flow 发生选择反馈循环；用户可继续通过 `Shift` 或 `⌘/Ctrl` 选择新节点组。
- 新增纯数据层回归覆盖内部边恢复、相对位置、项目内素材引用、待生成执行状态与连续粘贴命名；界面回归确认 Prompt 编辑时不劫持正常的复制/粘贴快捷键，且跨草稿不会粘贴旧节点组。Preview 全量测试共 49 项通过。jsdom MiniMap 的既有 `NaN` SVG 警告仍不影响断言，当前受限环境无法新启本地回环浏览器会话，因此浏览器实测留待开发机补录。

### 4.6 项目内参考图生成链路增量验证（2026-08-20）

- OpenAI 图像适配器现将无参考素材的请求发送到 generations，将当前项目的 PNG/JPEG/WebP 图片参考发送到 multipart edits；来源字节只在本地进程短暂存在，生成结果仍由 Core 写回项目素材存储。
- `gpt-image-2` 画幅路由按当前公开约束校验任意合法尺寸，因此画布已有的 `2048 × 1152` 与 Studio 预设均可通过路由；不满足 16px 倍数、像素或比例限制的尺寸会在创建任务前拒绝。
- 图像/视频镜头的参考素材选择层只新增当前 P0 真实路由可执行的 PNG/JPEG/WebP 图片。旧项目中已存在的不兼容引用会显示为可移除警告，不会被静默改写；角色、场景与本地素材引用等上下文节点继续保留项目内任意素材引用能力。
- Core 43 项、Preview 49 项回归，以及类型检查、Preview 生产构建和 `git diff --check` 均通过。未使用真实 API Key 或外部生成调用。

### 4.7 空白画布与抓手导航增量验证（2026-08-21）

- 空白画布的“故事脚本”快捷入口现在创建 `script` 上下文节点，而不是错误地创建文本节点；节点标题、编辑器标题和默认 Prompt 均与入口语义一致。
- 抓手工具下，React Flow 节点不再保留拖拽节点专用的 `nopan` 屏蔽类，因此用户可从节点表面开始平移；选择工具恢复该屏蔽类以保留直接拖动节点。节点表面也给出抓手/抓取中的视觉反馈。
- 新增组件回归覆盖上述两条行为。`npm run check`、Preview 50 项回归、Preview 生产构建和 `git diff --check` 均通过。未执行真实 API 调用。

### 4.8 触控板导航口径增量验证（2026-08-21）

- 明确将双指滑动定义为画布平移，避免与“缩放手势”的帮助文案冲突；双指捏合以及 `⌘/Ctrl` 加滚轮用于缩放。
- React Flow 同时显式接受 `Meta` 与 `Control` 作为缩放修饰键，因此 macOS 和其他桌面环境都有可预期的缩放入口；缩放按钮、数值输入与适配视图保持不变。
- 组件回归确认画布的无障碍说明和快捷键面板均反映上述手势。`npm run check`、Preview 50 项回归、Preview 生产构建和 `git diff --check` 均通过。受限环境无法监听本地端口，因此本增量未新增浏览器截图。

### 4.9 文本上下文节点密度增量（2026-08-21）

- 已采集的文本节点参考状态具有“图标 + 纵向创作入口”的卡片密度；此前本地文本节点只显示空图标，因此在未展开参数区时难以分辨其可承担的创作语义。
- 本地文本节点现展示独立产品语义下的“叙事文本、镜头描述、画面提示、声音意图”四个方向。它们是文本上下文可供下游镜头消费的内容类别；实际编辑仍通过点击节点后的同一 Prompt 编辑器完成，不伪造外部工作流或模型能力。
- 新增组件回归覆盖四个方向均被渲染。`npm run check`、Preview 51 项回归、Preview 生产构建和 `git diff --check` 均通过。当前受限环境无法新启可截图的本地服务，因此该可见增量仍等待开发机同视口复核；本文档最终结果继续为 `blocked`。

### 4.10 视频输出约束增量（2026-08-21）

- 视频镜头的“输出设置”现可直接修改项目文档中的画幅、时长（自动、5、10、15 秒）和音频意图（随提供方、需要音频、无音频）；新建视频镜头默认使用自动时长，避免把未由当前路由承诺的固定时长伪装为默认能力。不引入浏览器端密钥、在线模型选择或全局素材面板。
- 每项修改都经过既有共享 `updateNode` mutation，因此会保留失效传播、未保存反馈和撤销/重做链路；选择“自动时长”会移除显式时长约束，而不是伪造一个固定结果。
- 画布只记录用户的输出约束；在实际生成前，已配置的本地 BYOK 路由仍会验证是否有兼容能力，无法满足时返回安全错误。
- 已在组件回归中覆盖 5 秒到 10 秒、音频意图、画幅变化及恢复自动时长。`npm run check`、Preview 51 项回归、Preview 生产构建和 `git diff --check` 均通过。该弹层新增控件尚未在当前受限环境取得同视口浏览器截图，最终视觉结果仍为 `blocked`。

### 4.11 图像候选数量增量（2026-08-21）

- 图像镜头的输出设置新增 1、2、4 张候选约束；它写入共享项目文档的 `requirements.count`，与画幅一样会触发正常的失效传播和撤销/重做。视频节点不显示该控制，避免把单输出视频适配器伪装为多候选能力。
- OpenAI 图像适配器把可用的候选数映射到 Image API 的 `n`，并将响应中的每一张图独立落为当前节点的有序项目内输出；预览端提供“查看本次生成结果”选择层，不建立全局素材库或历史面板。
- 已完成 Core 路由/适配器/落库回归（46 项）、CLI 非回环参数回归（9 项）和 Preview 组件回归（52 项），并通过类型检查、生产构建和 `git diff --check`。当前执行环境仍不能监听回环端口，且未获得与参考状态配对的本地截图；因此本增量的视觉验收继续保持 `blocked`，不把组件通过记为视觉对齐完成。

### 4.12 脚本到镜头图增量（2026-08-21）

- 选中项目内“故事脚本”节点后，可将按行书写的分场（或一段 prose 中的句子）展开为最多 8 个本地视频镜头。每个镜头是普通可编辑节点，由 `dependency` 边接收脚本上下文，并以 `sequence` 边保留相邻镜头顺序；不会建立模板库、全局角色库或素材库。
- 点击展开时会一并写回尚未应用的脚本文本，因此创建出的镜头和后续 Codex/CLI 编辑都基于同一份项目文档。该操作只改变本地画布，不发起提供方调用。
- 新增纯规划、组件闭环及 Core 的“脚本上下文携带项目内参考素材进入视频请求”回归；Preview 当前共 58 项通过。当前受限环境无法监听本地端口，尚未补录该新增按钮的浏览器级截图；因此它的视觉验收仍保持 `blocked`。

### 4.13 合成顺序可见性增量（2026-08-21）

- 合成输出节点不再只按数组顺序罗列上游，而是从连接到它的镜头与显式 `sequence` 边计算并展示“镜头 A → 镜头 B”的项目内顺序。顺序不完整时会如实提示，避免把数组位置伪装为时间线。
- 脚本展开复用同一关系：每个镜头依赖脚本文本，相邻镜头建立 sequence 边，因此之后连接合成输出时已具备确定的顺序数据。
- Preview 纯模型和组件回归共 58 项通过。当前受限环境无法监听本地端口，这一产品特有的输出节点状态仍待浏览器级截图复核。

### 4.14 单节点框选与端口几何增量（2026-08-21）

- Shift 框选现在会将仅覆盖一个节点的结果同步回受控画布状态；此前代码只接纳多节点的 React Flow 选中变化，导致单节点框选可能在下一次渲染后丢失焦点。
- 参考基线中的端口圆点位于卡片边缘且尺寸紧凑。因此保留了 64px 的拖拽命中区，却把可见圆点中心收回卡片边缘，并从 20px 收敛为 11px，避免扩大节点间距中的视觉端口。
- 本轮应用内浏览器可读取到参考站的未登录落地页，但无法取得画布页截图；本地预览也仍受 `listen EPERM` 限制。因此这两项只完成了代码与回归核验，不将其标记为新的浏览器级视觉结论。Preview 59 项回归、类型检查、Preview 生产构建和 `git diff --check` 均通过。

### 4.15 脚本展开的三端共享增量（2026-08-21）

- 脚本展开从预览端局部逻辑收敛为 Core mutation。Studio 和 CLI 共同创建项目内视频镜头、脚本到镜头的 `dependency` 边，以及相邻镜头的 `sequence` 边；脚本文本可在展开前一并写回，整次展开只消耗一次项目和草稿 revision。
- CLI 新增 `script expand --project <dir> --node <script-node-id> [--limit 1..8] [--prompt <script>]`。Skill 使用该命令创建可继续编辑的镜头图，不通过手写项目 JSON 或隐式全局资源库完成规划。
- Core 50 项回归、Preview 59 项回归、类型检查、Preview 生产构建和 `git diff --check` 均通过。新增 CLI 展开用例通过；完整 CLI 测试中的两项预览桥接用例仍受当前环境的 `listen EPERM` 限制。该项属于数据与命令闭环，不作为新的浏览器级视觉验收结论。

### 4.16 选区与自动整理的原子布局写入（2026-08-21）

- 多节点拖动与“保留自动整理”不再逐卡调用 `updateNode`。新增共享 Core `moveNodes` mutation：先校验项目/草稿 revision、全部节点 ID 和坐标，再以一次事务写入完整坐标集合；因此不会出现半完成布局，也不会为一次手势消耗多条撤销记录。
- 布局变更只更新节点 `position`，不改变 `specRevision`、输入指纹、任务状态或生成结果。Studio 的单节点拖动、选区拖动和整理确认都走同一操作，CLI/Skill 继续消费同一项目文档而不需要全局角色库或素材库。
- Core 增加了成功批量移动与无效批次不污染源文档的回归。当前 Core 52 项、Preview 59 项回归、根目录类型检查、Preview 生产构建与 `git diff --check` 均通过。当前环境不能建立本地监听端口，因此这项的真实拖拽复核仍待可访问的开发机浏览器会话，不把自动化通过误记为新的视觉结论。

### 4.17 项目内分组帧增量（2026-08-21）

- 公开只读参考实例中可观察到低层级、无端口的分组帧：它位于普通媒体节点之后，标签呈“分组 N 个节点”，并以内容节点的边界形成一层背景结构。该状态只作为画布结构证据；本地实现不采用外部产品的名称、素材、代码或业务语义。
- Open Canvas 新增 `composition.role = "group"` 的布局实体。它只持久化同一草稿内至少两个 `memberNodeIds`，不携带 Prompt、素材引用、边、任务或输出；帧边界由成员节点动态推导。单独移动成员会同步帧锚点；删除成员会在同一 mutation 中更新或移除分组，默认标题同步更新成员数而自定义标题保持不变，从而不会保存悬空或误导性的分组信息。
- Studio 支持选中至少两个未分组节点后通过工具栏或 `⌘/Ctrl+G` 创建分组；点击分组帧仅选中成员且不展开参数编辑器，拖动帧通过一次 `moveGroup` 原子写入移动全部成员。CLI 可用 `group create --project <dir> --members <id,id,...>` 写入同一模型。角色、风格和素材仍是项目内上下文/引用节点，不新增任何跨项目资源库。
- Core 55 项、Preview 61 项回归通过；新增 CLI 分组用例通过。完整 CLI 测试中两个既有桥接用例仍因当前环境 `listen EPERM` 失败。当前浏览器策略同时禁止以本地构建文件替代回环服务，因此尚未获得与参考分组状态同视口的本地截图；本项仅标记为数据与组件闭环，视觉验收待可监听的开发机会话。

## 五、后续验收

后续迭代按同视口、同状态的方式继续核验默认态、选中态、添加菜单、端口连接、自动整理、缩放与窄视口。新增功能进入画布前，先确认其属于项目内创作能力，避免重新引入全局角色库或素材库。

## 六、本轮同视口设计 QA（2026-08-20）

### 比对输入与归一化

- **source visual truth path**：`https://www.liblib.tv/canvas?spaceId=5276920&projectId=357ecdb6f8614a2fa23773b7d779c0a1`
- **implementation screenshot path**：`http://127.0.0.1:4174/?qa=wide-anchor-fix`（当前会话中的浏览器渲染捕获）
- 历史截图基线：
  - `docs/qa/alignment-pass-2026-08-20-continuation/125-reference-clean-final-1280.png`
  - `docs/qa/alignment-pass-2026-08-20-continuation/126-local-clean-final-1280.png`
  - `docs/qa/alignment-pass-2026-08-20-continuation/127-clean-final-comparison-1280.png`
  - `docs/qa/alignment-pass-2026-08-20-continuation/128-reference-selected-after-fixes.png`
  - `docs/qa/alignment-pass-2026-08-20-continuation/129-local-selected-after-fixes.png`
  - `docs/qa/alignment-pass-2026-08-20-continuation/130-selected-comparison-after-fixes.png`
- 视口：两端均为 `1280 × 720` CSS px，`devicePixelRatio = 2`；浏览器截图输出均为 `1280 × 720` px，因此以同一密度的浏览器输出直接比较，不做缩放。
- 状态：默认多节点画布；点击左侧第一个图像节点后的选中态；图像节点含完成输出。
- 比较证据：每次结论均在同一浏览器操作中先后捕获参考图与本地图，再共同进行全视图比较；不是依据代码或两张独立截图回忆判断。当前会话复核了默认、选中、快捷工具栏、提示词聚焦、创建变体、画布平移和节点拖动。

### 当前同视口测量

| 区域 | 参考 | 本地 | 结果 |
| --- | --- | --- | --- |
| 默认图像节点 | `121,368,193×108` | `121,368,193×109` | 几何对齐；1 px 高度来自本地边框。 |
| 选中态内联编辑器 | `-113,482,660×191` | `-113,481,660×191` | 几何对齐；1 px 纵向差不影响控制层。 |
| 选中态快捷工具栏 | `-233,302,901×49` | `-233,302,901×49` | 几何、分组和右侧四枚紧凑操作位对齐。 |
| 快捷操作位 | `507/547/587/627` | `506/546/586/626` | 1 px 子像素取整差异。 |
| 文本节点编辑器 | `101,482,660×204` | `102,481,660×203` | 几何对齐；本地省略在线模型与额度字段。 |
| 添加节点菜单 | `397,约168,196×约481` | `397,168,196×481` | 锚点、宽度和纵向密度对齐；本地替换为完整的项目内节点调色板。 |
| 图像节点连接端口 | 输入 `121,422`；输出 `313,422` | 输入 `121,422`；输出 `314,422` | 端口锚点对齐；本地额外提供无障碍标签。 |
| 画布平移 | 节点和浮层同属画布坐标系 | 真实拖拽后节点、编辑器、工具栏同向平移 | 通过。 |
| 节点拖动 | 节点与随附 UI 同步移动 | 节点由 `121,368` 至 `161,395`，编辑器由 `-113,481` 至 `-72,509`，工具栏由 `-233,302` 至 `-193,329` | 通过。 |

### 五项保真面检查

- **字体与排版**：小字号节点标签、顶栏和工具栏使用本地系统无衬线字体栈，字重、单行截断和 `13 px` 工具栏密度与参考同级。外部品牌字样、模型名和在线状态不使用。
- **布局与间距**：深色点阵、左侧图像节点世界原点、曲线扇出边、顶栏、左下控制组和居中底栏均在同一坐标系中复核。图像选中态改为中心锚定，允许左侧自然溢出；工具栏和参数卡片的尺寸、圆角、阴影和偏移均按当前参考状态测量调整。
- **颜色与视觉令牌**：画布底色、低对比点阵、深灰节点表面、细边框、白色激活操作和蓝色连线在全视图下保持同级层次；没有引入外部品牌或积分语义色。
- **图像与图标**：本地演示图为 Open Canvas 本地资源，不热链外部素材；图标来自开源图标库。四个紧凑操作位可实际聚焦 Prompt、创建项目内编辑变体、下载本地输出和打开预览。
- **文案与内容**：角色设定、场景与风格、素材引用都是项目内节点；不出现全局“角色库”“素材库”“风格库”或历史入口。账户、额度、在线 Agent、远端模型字段属于明确排除项。

**Findings**

- 本轮没有未解决的 P0、P1 或 P2 视觉/交互问题。

### 实时窄视口复核（716 × 714）

- 同一可调浏览器表面以 `716 × 714` CSS px 重新打开本地画布；参考基线为 `docs/qa/fidelity-pass-2026-08-20/08-reference-default-716x714.png` 与 `05-reference-image-selected-716x714.png`。
- 默认态：参考与当前本地首图像节点均为 `48,290,188×106`；本地视口为 `30.2744%`，而不是桌面 `31%` 的 `121,368` 锚点。
- 选中态：当前本地节点仍在 `48,290`；`660×191` 内联参数区和 `901×49` 快捷栏自然向左溢出，不覆盖底部固定工具栏。与同状态参考基线共同核验后，节点、编辑器和快捷栏的相对关系一致。
- 交互：空白处拖动使节点整体从 `48,290` 移至 `102,314`；节点拖动使首节点从 `48,290` 移至 `68,304`，随附参数区同步移动，且画布未误平移。
- 响应式：在同一会话从 `716 × 714` 切换到 `1280 × 720` 时，首节点切换至 `121,368 / 31%`；切回后恢复 `48,290 / 30.2744%`。

其余可见差异均属于明确产品边界，而非待修复的画布保真问题：顶栏不含账号/积分/在线 Agent，底栏不含工具箱、素材库、角色库、历史或教程入口，图像参数不显示远端模型、额度或在线资产操作。

**Open Questions**

- 当前 P0 决策：不建设跨项目角色库或素材库；项目内需求由 `character`、`scene-style`、`asset-reference` 节点与 CLI 导入覆盖。
- 最终多轨剪辑和成片渲染属于后续 V2；当前剪辑节点承载可编辑的创作意图与关系图。

**Implementation Checklist**

1. [x] 在实时窄视口复核默认态、选中态、平移与节点拖动。
2. [x] 修正紧凑视口的图形初始锚点，并验证尺寸来回切换。
3. [x] 对照已归档的同状态参考基线与当前浏览器渲染，记录测量结果。
4. [x] 已重新运行全仓检查、测试、预览构建和 `git diff --check`。

**Follow-up Polish**

- [P3] 默认图像节点可在不影响卡片几何的前提下继续收敛 1 px 高度差。
- [P3] 在独立产品语义不变的前提下，继续细化本地顶栏的字体光学重量和图标线宽。

### 本轮比较历史

1. **P1：宽图默认视图随窗口 fit 导致首节点漂移。**修复为稳定世界原点锚点；修复后参考与本地首图像节点均为 `121,368`。
2. **P1：选中图像节点的参数卡片被安全内缩。**移除靠边时的 `start/end` 重定位并收敛媒体编辑器为 `660×191`；修复后参考为 `-113,482,660×191`，本地为 `-113,481,660×191`。
3. **P1：单击图像节点后本地没有默认显示快捷工具栏。**恢复默认快捷栏与右侧四个操作位；修复后两端均为 `-233,302,901×49`，各紧凑操作位只有 1 px 取整差。
4. **P2：节点稍向下拖动时本地过早将参数区翻转到上方。**浮层避让阈值统一为已测的 `191 px` 参数卡片高度；拖动后参数区继续按下方跟随，不提前翻转。
5. **P1：图像/视频输出规格仅是静态读数。**将规格读数改为保持同等默认密度的按需设置入口；图像可选 `16:9`、`9:16`、`1:1` 的本地可执行尺寸，视频可选本地适配器支持的画幅。改动通过同一文档 mutation、依赖失效和撤销链路；真实浏览器中已验证选择 `9:16` 后从 `2048 × 1152` 更新至 `1008 × 1792`、清除过期结果并展示保存反馈。
6. **P2：宽图在首帧短暂展示为 50% 的紧凑视图。**将 React Flow 的初始 viewport 直接设为已测工作锚点；重新打开本地画布后首帧即为 `31%`、首图像节点位于 `121,368`，不再等待后续 fit effect 跳转。
7. **P2：下方视频节点的输出设置会被上方快捷栏或顶栏遮挡。**将设置面板提升为画布层 portal，并根据节点、快捷栏和固定控制层的位置选择同侧、快捷栏下方或节点左侧的空闲区域；真实浏览器中已验证视频 `9:16` 控件与“时长与音频”说明完整可点，且更新后展示 `9:16 · 5 秒` 与节点失效反馈。
8. **P1：聚焦节点没有可靠的删除闭环。**新增共享核心 `deleteNode` mutation 与 CLI `node delete`；画布在焦点节点上捕获 `Delete/Backspace`，避免自定义节点表面与 React Flow 内部选择状态不一致。删除会清理节点、关联边与任务，让直接受影响下游失效；未被任何草稿引用的该任务输出从文档索引移除，仍被其他草稿引用的不可变素材保留为 `deleted-job` 来源。真实浏览器已验证删除与撤销恢复。
9. **P1：节点调色板缺少可表达最终编排的输出节点。**新增“合成输出”节点，并将其与图片/视频生成节点明确区分：不伪造提供方、Prompt 或输出规格，而是显示上游节点、完成数量、连接关系和“剪辑工作区接入成片渲染”的真实边界。真实浏览器已验证创建和连线后的输入状态更新。
10. **P1：镜头不能直接消费项目内导入的参考素材。**在图像和视频镜头的内联参数区新增按需打开的“本地参考素材”选择层，直接写入镜头的 `inputAssetIds`，复用共享 mutation 的失效与撤销语义。选择层只枚举当前项目已导入素材，并明确提示新增文件仍通过 CLI 导入；真实浏览器已验证关联、数量回显和结果转为待生成。
11. **P1：CLI 的打开链接没有真正加载本地项目。**新增单项目、回环地址、令牌保护的预览桥接；它只暴露校验后的 `project.json` 与该项目已声明的资产 ID，不暴露目录、任意路径或凭证。Studio 通过桥接加载项目，只有点击保存才附带已持久化 revision 写回；冲突、无效文档和新增资产索引均被拒绝。真实浏览器已验证 Prompt 保存后 CLI 读回相同版本。
12. **P1：紧凑视口沿用了桌面宽图锚点。**将宽图 framing 改为按视口选择的已测配置：桌面使用 `121,368 / 31%`，`≤800 px` 使用 `48,290 / 30.2744%`。锚点计算下沉为可单测的纯函数；真实浏览器已验证默认、选中、平移、拖动和往返尺寸切换。

### 自动化与运行时复核

- 当前变更后已执行全仓检查、Core 43 项与 Preview 51 项回归、Preview 生产构建和 `git diff --check`。CLI 的两条本地桥接测试仍需在可监听回环端口的开发机补录；当前受限环境在监听阶段返回 `listen EPERM`。
- 浏览器实际操作已验证：点击聚焦、Prompt 聚焦、创建编辑变体、画布平移、节点拖动、内联编辑器与快捷工具栏同步、图像节点画幅更新后的结果失效与保存反馈、节点删除后的撤销恢复、合成输出节点创建与镜头连线、图像镜头直接关联项目内参考素材，以及 CLI 打开项目后的 Prompt 保存回读。
- 浏览器可见运行时操作未出现错误；jsdom 中 React Flow MiniMap 仍会输出非阻塞的 SVG `NaN` 警告，实际浏览器渲染未复现。

### 本次增量的可视化复核状态（2026-08-20）

- 节点组复制/粘贴是在上述同视口基线之后新增的交互；其数据和组件回归均已通过，但还没有新的浏览器截图证明快捷键与选区在真实页面中呈现正常。
- 当前受限环境拒绝 Vite 在 `127.0.0.1:4174` 监听（`listen EPERM`），因此无法打开本地实现捕获同状态截图；已连接的参考页也处于未登录落地页，不能采集项目画布状态。
- 已归档的默认、选中、菜单、窄视口、平移和节点拖动对齐证据仍然有效；本次状态只表示“当前增量尚未完成浏览器级可视化复核”，不将端口受限误判为产品断言失败。

final result: blocked

## 四类节点首版增量验收（2026-09-15）

本轮独立记录见 [四类节点本地验收](docs/qa/four-nodes-2026-09-15/README.md)。158 项测试、全仓检查和构建通过，已补音频播放、下载校验、窄屏设置与保存读回的浏览器证据。新建入口收敛为四类，旧项目兼容保留。为保证窄屏可操作，本轮以编辑器水平边界限制替代历史的靠边裁切规则；不改变画布节点坐标。真实提供方调用仍未验收，不将本轮本地通过解释为整份历史 QA 全部完成。
