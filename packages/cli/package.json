{
  "name": "@open-canvas/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "open-canvas": "./bin/open-canvas.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build --workspace @open-canvas/core && node --import tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "@open-canvas/core": "0.1.0"
  }
}
